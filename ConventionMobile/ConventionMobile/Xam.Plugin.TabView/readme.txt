This is the 1.0.4 source code for the https://github.com/chaosifier/TabView plugin.

The reason the direct source is being included is because the NuGet package requires 
the use of an embedded (out of date) CarouselView plugin, which is causing tabbing bugs on iOS.

If the source plugin is ever updated, feel free to include the direct NuGet package and remove this source.